name: Build with vcpkg

on:
  push:
    branches:
      - master
      - v*

    paths:
      - .github/workflows/build-vcpkg.yml
      - cmake/**
      - src/**
      - CMakeLists.txt
      - CMakePresets.json
      - vcpkg.json

  pull_request:
    paths:
      - .github/workflows/build-vcpkg.yml
      - cmake/**
      - src/**
      - CMakeLists.txt
      - CMakePresets.json
      - vcpkg.json

jobs:
  build-vcpkg:
    name: ${{ matrix.os }}-${{ matrix.buildtype }}-luajit=${{ matrix.luajit }}
    runs-on: ${{ matrix.os }}-latest
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu, macos, windows]
        buildtype: [Debug, Release]
        luajit: [on, off]

    env:
      VCPKG_BUILD_TYPE: release

    steps:
      - uses: actions/checkout@v7

      # ubuntu-latest defaults to GCC 13, which lacks the C++23 support the
      # tree needs, so pick the GCC 14 the image also ships. macOS builds
      # with the Xcode clang instead: Homebrew's GCC cannot parse the macOS
      # 26 SDK headers (mach/message.h uses clang-only static_assert
      # macros), so every dependency that touches them fails to compile.
      - run: echo CXX=g++-14 >> $GITHUB_ENV
        if: ${{ matrix.os == 'ubuntu' }}

      - name: Get latest CMake
        # Using 'latest' branch, the latest CMake is installed.
        uses: lukka/get-cmake@latest

      - name: Run vcpkg
        uses: lukka/run-vcpkg@v11

      # Configure before CodeQL starts tracing. The vcpkg toolchain builds the
      # dependencies from source during project(), and everything compiled inside
      # the tracing window ends up in the CodeQL database.
      # The unity build is disabled on the CodeQL leg so that alerts are reported
      # against src/*.cpp instead of the generated Unity/unity_*.cxx files.
      - name: Configure with CMake
        uses: lukka/run-cmake@v10
        with:
          configurePreset: vcpkg
          configurePresetAdditionalArgs: "['-DUSE_LUAJIT=${{ matrix.luajit }}', '-DENABLE_UNITY_BUILD=${{ (matrix.os == 'ubuntu' && matrix.buildtype == 'Debug') && 'OFF' || 'ON' }}']"

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: cpp
          # For more details on CodeQL's query packs, refer to: https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
          queries: +security-extended,security-and-quality
        if: ${{ matrix.os == 'ubuntu' && matrix.buildtype == 'Debug' }}

      - name: Build with CMake
        uses: lukka/run-cmake@v10
        with:
          buildPreset: vcpkg
          buildPresetAdditionalArgs: "['--config', '${{ matrix.buildtype }}']"

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:cpp"
          output: sarif-results
          upload: failure-only
        if: ${{ matrix.os == 'ubuntu' && matrix.buildtype == 'Debug' }}

      # paths-ignore has no effect on a compiled language that is analysed with a
      # build, so the dependency headers under build/vcpkg_installed and the
      # generated files under build/src have to be dropped from the results.
      - name: Filter out dependency and generated code
        uses: advanced-security/filter-sarif@v1.1
        with:
          patterns: |
            +**
            -build/**
            -vcpkg/**
          input: sarif-results/cpp.sarif
          output: sarif-results/cpp.sarif
        if: ${{ matrix.os == 'ubuntu' && matrix.buildtype == 'Debug' }}

      - name: Upload CodeQL results
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: sarif-results/cpp.sarif
          category: "/language:cpp"
        if: ${{ matrix.os == 'ubuntu' && matrix.buildtype == 'Debug' }}

      # upload-artifact v4 and later reject a second upload to an existing
      # artifact name, so the binaries and the datapack have to be collected
      # into the workspace and uploaded by a single step.
      # The vcpkg preset builds into ${sourceDir}/build, so the binary has to
      # be pulled out before the workspace is pruned down to the datapack.
      - name: Prepare datapack contents
        run: |
          build=build/${{ matrix.buildtype }}
          cp $build/tfs $build/tfs.exe $build/*.dll . 2>/dev/null || true
          find . -mindepth 1 -maxdepth 1 ! -name data ! -name config.lua.dist ! -name key.pem ! -name LICENSE ! -name README.md ! -name schema.sql ! -name tfs ! -name tfs.exe ! -name '*.dll' -exec rm -r {} \;
          [ -f tfs ] || [ -f tfs.exe ] || { echo "::error::no tfs binary found in $build"; exit 1; }
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: tfs-${{ matrix.os }}-${{ matrix.buildtype }}-luajit=${{ matrix.luajit }}-${{ github.sha }}
          path: ${{ github.workspace }}
